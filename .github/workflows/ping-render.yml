name: Keep Render Awake

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping CRM backend
        run: |
          curl -fsS https://crm-o52e.onrender.com/ || exit 1

      - name: Ping Reimbursement backend
        run: |
          curl -fsS https://reimbursement-wesa.onrender.com/ || exit 1

